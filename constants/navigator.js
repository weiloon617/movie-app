/**
 * stack header bar config
 */
export const stackHeaderConfig = {
  headerBackTitle: "",
  headerTitle: ""
};

/**
 * tab bar config
 */
export const tabBarConfig = {
  labelStyle: { fontSize: 14 },
  tabStyle: { padding: 5 }
};
